spring:
    datasource:
        url: jdbc:h2:mem:db
        username: db
        password: db
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect

    h2:
        console:
            enabled: true
